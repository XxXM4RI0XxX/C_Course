#include <stdio.h>

void main(){

    int a;

    printf("\nDame el valor de a\n");
    scanf("%d",&a);//Recive valores. '%d', recibe un entero, si fuera cadena seria '%s', caracter '%c', etc...
    //&x, x es la variable a la que se le asignara el valor.
    printf("el valor de a = %d", a);
    printf("Dame el valor de b\n");
    printf("Dame el valor de b");

}