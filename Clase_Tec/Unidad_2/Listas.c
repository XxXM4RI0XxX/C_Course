#include <stdio.h>
#include <stdlib.h>

// *cabeza(es la raiz) -> *bagonos

typedef struct nodo
{
    int valor;
    struct nodo *sig;
}nodo;

nodo *raiz = NULL;
//Insertar, sacar, ordenar
void main()
{
    
}