#include <stdio.h>
#include <stdlib.h>

// *cabeza(es la raiz) -> *bagonos

typedef struct elem
{
    int valor;
    struct elem *next;
}elem;

elem *raiz = NULL;
//Insertar, sacar, ordenar
void main()
{
    
}